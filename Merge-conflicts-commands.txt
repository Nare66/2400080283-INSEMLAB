# Create two branches
git checkout -b feature1
# Alice modifies app.js
# Commit Alice's changes
git commit -am "Alice's changes to app.js"

git checkout main
git checkout -b feature2
# Bob modifies the same function in app.js
# Commit Bob's changes
git commit -am "Bob's changes to app.js"

# Merge feature1 into main
git checkout main
git merge feature1

# Merge feature2 into main (conflict occurs)
git merge feature2

# Edit app.js to resolve conflict, remove <<< >>> markers
git add app.js
git commit -m "Resolved merge conflict in app.js"